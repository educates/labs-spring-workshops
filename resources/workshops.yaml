apiVersion: training.educates.dev/v1beta1
kind: Workshop
metadata:
  name: lab-creating-a-spring-app
spec:
  title: Creating a Spring Boot application
  description: A workshop introducing the start.spring.io web site.
  workshop:
    image: jdk11-environment:*
    files:
    - image:
        url: $(image_repository)/labs-spring-quickstarts/lab-creating-a-spring-app-files:latest
      includePaths:
      - /workshop/**
    - path: .m2
      image:
        url: ghcr.io/vmware-tanzu-labs/lab-spring-boot-on-k8s-maven:latest
  session:
    resources:
      memory: 1Gi
    patches:
      automountServiceAccountToken: false
    applications:
      terminal:
        enabled: true
        layout: split
      editor:
        enabled: true
    dashboards:
    - name: Initializr
      url: $(ingress_protocol)://initializr-$(session_namespace).$(ingress_domain)
    ingresses:
    - name: initializr
      protocol: http
      host: initializr.$(workshop_namespace).svc.cluster.local
      port: 8080
    - name: demo
      port: 8080
  environment:
    objects:
    - apiVersion: apps/v1
      kind: Deployment
      metadata:
        name: initializr
        labels:
          app: initializr
      spec:
        replicas: 1
        selector:
          matchLabels:
            app: initializr
        template:
          metadata:
            labels:
              app: initializr
          spec:
            containers:
            - name: dashboard
              image: ghcr.io/vmware-tanzu-labs/educates-spring-initializr:2.0
              imagePullPolicy: Always
              ports:
              - name: 8080-tcp
                containerPort: 8080
                protocol: TCP
    - apiVersion: v1
      kind: Service
      metadata:
        name: initializr
        labels:
          app: initializr
      spec:
        type: ClusterIP
        selector:
          app: initializr
        ports:
        - name: 8080-tcp
          port: 8080
          protocol: TCP
          targetPort: 8080
---
apiVersion: training.educates.dev/v1beta1
kind: Workshop
metadata:
  name: lab-containerizing-spring
spec:
  title: Containerizing a Spring Boot application
  description: A workshop describing how to run a Spring Boot application in docker.
  workshop:
    image: jdk11-environment:*
    files:
    - image:
        url: $(image_repository)/labs-spring-quickstarts/lab-containerizing-spring-files:latest
      includePaths:
      - /workshop/**
  session:
    resources:
      memory: 1Gi
    patches:
      automountServiceAccountToken: false
    applications:
      terminal:
        enabled: true
        layout: split
      editor:
        enabled: true
      docker:
        enabled: true
    ingresses:
    - name: demo
      port: 8080
